"""Кросс-слойные утилиты, не относящиеся ни к боту, ни к админке.

* `rate_limit.py` — in-memory sliding-window limiter (login, /scan).
* `formatting.py` — единый формат дат и длительностей для UI и шаблонов.

Сюда попадает только то, что используют сразу несколько слоёв и что
не имеет внешних зависимостей кроме stdlib.
"""
