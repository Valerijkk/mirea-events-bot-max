"""Слой MAX-бота: хендлеры, клавиатуры, тексты, отправка сообщений.

* `instance.py` — глобальные `bot` и `dp` (модульные синглтоны).
* `client.py` — низкоуровневый HTTP-клиент к MAX Bot API.
* `dispatcher.py` — long-polling и роутинг update'ов в хендлеры.
* `handlers.py` — реакция на сообщения и нажатия на inline-кнопки.
* `keyboards.py` — конструкторы inline-клавиатур (формат МАКС).
* `texts.py` — все пользовательские строки (один источник правды для текстов).
* `notifications.py` — функции отправки сообщений и картинок.
"""
