# --- Веб-фреймворк и сервер ---
# fastapi: 0.121+ тянет starlette>=0.49.1 (закрывает CVE-2024-47874,
# CVE-2025-54121, CVE-2025-62727 — все DoS на парсере форм/мультипарт).
fastapi==0.121.0
uvicorn[standard]==0.32.0

# --- База данных ---
sqlalchemy==2.0.36
psycopg2-binary>=2.9

# --- Валидация и конфигурация ---
pydantic[email]==2.9.2
pydantic-settings==2.6.0

# --- Планировщик задач (напоминания) ---
apscheduler==3.10.4

# --- Утилиты предметной области ---
# iCalendar/Google Calendar ссылки генерируем своим минимальным кодом
# (app/services/ics.py) — сторонняя ics ломалась на Python 3.13.
qrcode[pil]==8.0

# --- Авторизация админки ---
# PyJWT: активно поддерживаемая альтернатива python-jose. python-jose заброшен
# (последний релиз 2024), тянет pyasn1<0.5 с открытым CVE-2026-30922.
PyJWT==2.12.0
passlib[bcrypt]==1.7.4
# bcrypt пинуется отдельно: новые версии (>=4.1) ломают passlib 1.7.4.
bcrypt==4.0.1

# --- HTTP-клиент и инфраструктура ---
httpx==0.27.2
# python-dotenv: 1.0.1 → 1.2.2 (CVE-2026-28684).
python-dotenv==1.2.2
